# Release version source correction / v0.12.29.11

## Baseline
- Main SHA: 1e34eafcf2a7416bd44387e49099c15f1683ff73
- Package/lock release: v0.12.29.10
- Production deployment still received APP_VERSION 0.12.29.7.
- Direct inspection found the actual `wrangler.toml` value is still 0.12.29.7; only its comment had been updated.
- Existing release test was too permissive because its APP_VERSION regex also matched the commented line.

## Fix
1. Set the actual `[vars].APP_VERSION` to v0.12.29.11.
2. Bump package/lock/changelog and release assertions to v0.12.29.11.
3. Strengthen version tests to match the active TOML assignment, not comments.
4. Keep deploy workflow without `--keep-vars`; Wrangler config remains the source of truth.
5. Run full CI, merge exact tested SHA, then production attestation.

## Acceptance
- CI verify and share-browser both pass.
- Test cannot pass when APP_VERSION exists only in a comment.
- Production /api/health returns appVersion 0.12.29.11 and exact main buildSha.
