## 0.13.1.0

- 统一考试范围：单科、多科、六科均为同一 Exam，通过 subjectSet 表示本次实际考试科目。
- 保存成功自动关闭编辑器并恢复原阅读位置；保存失败保留编辑器与草稿。
- 正常考试列表不再展示已删除考试；后端删除/恢复能力保留。
- 移动端考试编辑采用全屏滚动与固定操作区，兼顾保存与修改。
- Schema 1、KV key 与 Share v2 URL 保持兼容。

## 0.13.0.0

- 建立 v0.13 架构收敛边界：数据事实 → 语义 → 人话阅读 → 应用调度 → UI。
- 新增 canonical client domain facade，统一页面对成绩、比较、轨迹和阅读语义的入口。
- 修正“自动比较”必须跟随实际展示指标的架构规则；默认展示分数时比较也使用分数，显式选择排名时仍使用排名。
- 不改变成绩数据 Schema 1、Workers KV、Share v2 或公开分享链接。

## 0.12.30.0

- 从学生、家长、教师三个视角审查同一个“我的高三”，不新增角色入口或角色专属页面。
- 统一成绩阅读与比较语义：默认展示分数时优先按分数比较；明确单场分享、历次分享和固定内容的实际更新范围。
- 保存考试后明确显示已记录科目、总分和排名状态；六科已齐但学校公布总分待补时不再笼统标记为未完成。
- 不改变成绩数据 Schema 1、Workers KV 或既有分享链接兼容规则。

## 0.12.29.11

- 修正 Worker 发布版本的实际 Wrangler 配置值，并收紧版本契约测试，防止注释中的版本号掩盖真实运行变量。
- 保持非敏感变量由 Wrangler 配置统一管理，生产继续用精确 buildSha 收口。

## 0.12.29.10

- 修正生产部署的变量来源：不再保留 Cloudflare Dashboard 中旧的 Worker 非敏感变量，正式部署以仓库 Wrangler 配置为准。
- 继续通过 `secrets-file` 保留加密密钥，并用精确 buildSha 做生产收口验收。

## 0.12.29.9

- 修复 Cloudflare Worker 部署版本契约：在保留既有变量的同时，显式写入本次测试提交对应的 APP_VERSION，避免生产继续保留旧版本号。
- 增加部署工作流回归契约，并继续要求生产同时核验 appVersion 与精确 buildSha。

## 0.12.29.8

- 修订分享页阅读结构：总成绩在当前考试之后补充“历次总成绩”，历史记录可直接进入对应考试的时间轴详情。
- 时间轴严格按每场考试自身的总分、校内排名和班级排名展示，不从最近一次考试继承事实；历史分享字段继续严格受白名单控制。
- 单科默认阅读优先显示实际记录的分数，并在最新考试没有该科记录时定位到最近一次真正记录该科的考试。
- 增加多考试分享、字段白名单、单科实际记录和移动端可用性的回归验收；不改变 Schema 1、KV、Share v2 或 API 契约。

## 0.12.29.7

- 修正品牌更名后的发布回归契约：遗漏的 Worker APP_VERSION 断言同步到当前版本。
- 保持“我的高三”品牌、成绩事实、认证、分享、KV、Schema 1 与 API 语义不变。

## 0.12.29.6

- 产品品牌由“高三坐标”更名为“我的高三”，统一浏览器标题、登录/引导、家庭入口、当前 README 与架构标题。
- 保留“轨迹”“时间轴”等功能概念；不改变成绩事实、比较算法、认证、分享、KV、Schema 1 或 API。
- 同步 package、lockfile、Worker APP_VERSION 与品牌回归测试。
- 历史版本记录中的“高三坐标”保留为历史事实，不改写。

## 0.12.29.5

- 修正生产部署配置契约：显式设置 `[observability.logs] enabled = false`，兼容当前 Wrangler，并继续关闭 invocation logs。
- 不改变业务逻辑、成绩数据 Schema 1 或认证/分享语义。

## 0.12.29.4

- 修正浏览器回归契约：单科“默认”视图优先展示校内位置，因此分数变化改在总成绩页六科行验收；单科页继续验收实际记录。
- 同步发布版本契约到 v0.12.29.4。

## 0.12.29.3

- 修正发布回归测试中的正则转义，使 Worker `APP_VERSION` 的实际配置行能够被 Node 测试准确匹配。
- 最终版本契约统一到 v0.12.29.3；不改变业务数据与认证语义。

## 0.12.29.2

- 修正 v0.12.29.1 的安全发布契约测试，使实际 Worker `APP_VERSION` 精确锁定到当前修订版本。
- 同步 Worker 注释与实际版本，避免版本契约再次漂移。

## 0.12.29.1

- 修订公开分享页总分变化文案：从紧凑的“+15 分”改为“比上一场高 15 分”，与私有成绩阅读保持同一人话语义。
- 不改变成绩事实、比较资格、分享白名单、认证或 Schema 1。

## 0.12.28.19

- 修正上一轮发布契约回归测试自身的版本断言错误：测试现在严格匹配实际 `APP_VERSION` 配置行，而不是注释。
- Worker、package、lockfile、测试契约统一到 v0.12.28.19。
- 不改变 v0.12.28.17 的恢复凭证安全修复，不改变成绩数据 Schema 1。

## 0.12.28.18

- 修正 v0.12.28.17 合并后的 Worker `APP_VERSION` 发布契约同步错误。
- 强化版本回归测试，要求精确匹配实际 `APP_VERSION` 配置行，避免注释中的旧版本误满足测试。
- 不改变安全逻辑与成绩数据 Schema 1。

## 0.12.28.17

- 完成代码审查后的恢复凭证一次性生命周期收口：恢复码、恢复链接在成功修改账户状态后正式 consume，失败时释放 claim。
- 删除密码修改流程中重复且未使用的 KDF iteration 计算，统一由 `passwordIterations(env)` 提供策略。
- 增加本轮安全代码审查计划与后续架构项记录。
- 不改变成绩数据 Schema 1。

## 0.12.28.16

- 将部署版本回归改为动态契约校验，避免安全修订因历史硬编码版本号反复阻断。
- 发布版本统一为 v0.12.28.16；不改变安全逻辑与成绩数据 Schema 1。

## 0.12.28.15

- 修订发布契约：通过标准 Contents API 统一 Worker / package / lockfile。
- 将重复的版本硬编码回归改为契约式校验，避免后续版本同步漂移。
- 不改变安全逻辑与成绩数据 Schema 1。

## 0.12.28.14

- 统一最后遗留的 Worker、测试与进度版本契约到 v0.12.28.14。
- 不改变安全逻辑与成绩数据 Schema 1。

## 0.12.28.13

- 修正最后的发布契约同步：统一 Worker、package、lockfile、测试和进度记录为 v0.12.28.13。
- 不改变此前安全修复与成绩数据 Schema 1。

## 0.12.28.12

- 修订安全回归版本契约：同步剩余历史安全测试的正式发布版本断言。
- 不改变安全实现或成绩数据 Schema 1。

## 0.12.28.11

- 修订安全回归测试契约：v020 入口已拆分为 gateway 后，测试改为检查真实 gateway。
- 修正部署版本断言，统一 Worker / package / lock / tests 为 v0.12.28.11。
- 保留此前全部安全修复，不改变成绩数据 Schema 1。

## 0.12.28.10

- 修订发布契约：统一 Worker APP_VERSION 与 package/lock/test 版本为 v0.12.28.10。
- 修正上一版遗留的 APP_VERSION 契约不一致，不改变安全逻辑或成绩数据 Schema 1。

## 0.12.28.9

- 测试架构修订：抽出可由 Node 直接加载的 v0.20 gateway 模块，生产入口继续单独导出 SQLite Durable Object。
- 修正 v0.20 集成测试的运行入口，避免 Node 对 `cloudflare:` 内置模块的非 Worker 运行时限制。
- 不改变生产 Worker 路由、认证与成绩数据 Schema 1。

## 0.12.28.8

- 测试修订：v0.20 集成测试改为通过真实 v0.20 gateway 入口运行，确保新增 Session `jti` 撤销校验本身被覆盖。
- 不改变生产运行时与成绩数据 Schema 1。

## 0.12.28.7

- 测试修订：为 Bootstrap、家庭管理和 v0.20 集成链路提供显式的一次性凭证门闩测试绑定。
- 生产 Worker 的一次性凭证协调实现不变；不改变成绩数据 Schema 1。

## 0.12.28.6

- 安全修复：补齐 v0.20 API 网关的 Session `jti` 撤销校验，阻断普通退出后的敏感接口旁路。
- 增加对应回归测试；不改变成绩数据 Schema 1。

## 0.12.28.5

- 修订架构与安全边界文档：准确描述 Session 的服务端 `jti` 记录及 Workers KV 撤销传播边界。
- 不改变运行时安全逻辑与成绩数据 Schema 1。

## 0.12.28.4

- 安全修订：补齐临时 Bootstrap 初始化本身的一次性并发保护。
- 增加 Bootstrap 竞态回归测试；生产仍默认关闭该入口。
- 保留 v0.12.28.3 全部安全修复，不改变成绩数据 Schema 1。

## 0.12.28.3

- 修订安全发布版本：修正现有测试环境对一次性 Bootstrap 的显式配置，并保持 Worker 生产入口默认关闭。
- 修正 Durable Object 引入后 Node 集成测试的入口依赖；不改变生产 Worker 行为。
- 保留 v0.12.28.2 全部安全修复，不改变成绩数据 Schema 1。

## 0.12.28.2

- 修订发布版本：修正安全回归测试夹具，使 Bootstrap 只在测试环境显式开启。
- 保留 v0.12.28.1 的全部安全修复，不改变成绩数据 Schema 1。

## 0.12.28.1

- 修订发布版本：延续 v0.12.28 安全收口，修正版本契约并保留全部安全修复。
- 本次不改变成绩数据 Schema 1 与业务数据语义。

## 0.12.28

- 安全修复：阻断持久化考试名称进入 DOM HTML sink 的 Stored XSS 路径。
- 安全修复：邀请、恢复码、恢复链接使用 SQLite-backed Durable Object 串行化一次性凭证消费，降低并发重放风险。
- 安全修复：Secret Share 新链接改用 URL fragment 携带原始 token，并通过同源 POST redeem；继续兼容旧格式。
- 安全修复：普通退出现在服务端撤销当前 session；Bootstrap 建户入口默认关闭，并且启用后只允许一次成功建户。
- 安全修复：分享创建、撤销、兑换增加限流；429 正确返回 Retry-After。
- 安全修复：关闭 Workers invocation logs，减少敏感分享凭证进入默认请求日志的机会。
- 本轮不改变成绩数据 Schema 1；Durable Object 只承担一次性凭证协调。

## 0.12.27.1

- 修复 Cloudflare Web Crypto 不支持超过 100000 次 PBKDF2 iterations 导致登录失败的问题。
- 将生产密码 KDF 参数调整为 Cloudflare 当前运行时可执行的 100000 次，并保留旧密码兼容与 lazy rehash。

## 0.12.27

- 安全加固：提升密码 KDF 基线并支持旧密码登录后自动升级 hash。
- 增加登录、恢复、邀请等敏感入口限流与统一 429 响应。
- 新生成的邀请/恢复令牌使用独立密码学域，同时兼容旧 token locator。
- 新生成 Secret Share 使用版本化 token locator，同时保留历史分享链接兼容。
- 收紧安全回归与 CI/CD 供应链基线，不改变成绩、家庭、分享业务语义。

## 0.12.26

- 修复 Cloudflare Worker 部署工作流的触发边界：只响应 main 分支完成的 CI，PR 分支不会再取消正在进行的生产部署。
- 保留“只部署已通过 CI 的精确 SHA”以及部署后版本、构建 SHA attestation。
- 不改变成绩事实、比较算法、Schema 1、Workers KV、Share v2 或 API 契约。

## 0.12.25

- 在 v0.12.24 的信息层级收口基础上，删除已不再使用的重复轨迹阅读代码，避免同一事实在首页出现两套相近解释。
- 删除不再需要的独立视觉源文件，继续保持单一运行时 CSS bundle；补充分享页紧凑比较回归。
- 本次不改变成绩事实、比较算法、Schema 1、Workers KV、Share v2 或 API 契约。

## 0.12.24

- 收敛全站信息层级：当前考试与当前成绩保持主视觉，“和以前相比”改为窄阅读信息块，只有有实际内容的科目变化才出现。
- 重整总成绩页为“这次考试 → 和以前相比 → 值得回看的科目（有内容才显示）→ 这次成绩 → 历次考试”的单一阅读流，去掉重复的轨迹解读区。
- 分享页与时间轴减少重复分析说明，比较只在有用时出现；单次记录继续保留必要的记录状态说明。
- 新增 v0.12.24 响应式视觉治理层，统一 PC / Pad / Android 的阅读宽度、间距与触控目标，不改变成绩事实、比较算法、Schema 1、Workers KV、Share v2 或 API 契约。

## 0.12.23

- 将共享产品语言语义真正接入成绩、单科、考试详情、时间轴与概览页面，减少页面各自解释同一事实。
- 统一总分、六科合计、部分科目小计、校内/班级/联考排名和比较状态的用户语言。
- 删除主要运行页面中的“整体位置、变化来源、可比记录、同类别”等内部分析式表达，空分析模块在没有有效信息时不再占位。
- 保持成绩事实、比较算法、Schema 1、Workers KV、Share v2 与 API 契约不变。

## 0.12.22

- 建立全站产品语言治理契约：统一总分、六科合计、部分科目小计、排名范围、时间语义、比较状态与分享模式。
- 建立数据事实 → 语义状态 → 人话文案的共享边界，后续页面不得各自解释同一数据。
- 建立全站页面语言审计矩阵、AI/内部术语治理规则和断网接手进度文件。
- 新增纯函数语义格式化层与回归测试；本阶段不改变成绩事实、比较算法、Schema 1、Workers KV、Share v2 或 API 契约。

## 0.12.21

- 继续治理全站用户语言：把“可比/口径/位置向前后”等分析式表达收敛为可以核对的考试、成绩和排名事实。
- 明确“最近一次考试”和“最近一次有记录的成绩”的时间语义，时间轴、单科历史与分享页不再用“最新”承担时间含义。
- 持续更新分享统一说明“以后新增的考试也会显示在这里”，快照继续使用“固定当前内容”。
- 不改变成绩事实、比较算法、Schema 1、Workers KV、Share v2 或 API 契约。

## 0.12.20

- 清理全站最后两处主要用户文案中的“同口径 / 指标”分析术语，改为“相近考试 / 比较方式”，让首页阅读继续保持事实导向。
- 不改变成绩事实、比较算法、Schema 1、Workers KV、Share v2 或 API 契约。

## 0.12.19

- 在全站产品语言治理基础上修正现有文案回归契约，确保“目前的记录”“固定当前内容”“页面只显示你选择分享的内容”等用户语言与测试一致。
- 本次版本继续不改变成绩事实、比较算法、Schema 1、Workers KV、Share v2 或 API 契约。

## 0.12.18

- 全站用户文案收敛：统一“最近一次 / 历次考试 / 历次成绩 / 校内位置”等核心词义，减少“最新 / 当前 / 最近”混用。
- 将“基线、指标、口径、白名单、自动选择、成长轨迹”等内部或分析型术语从主要用户界面改为直接描述用户能看到的事实和操作。
- 分享管理与公开分享统一使用“固定当前内容”“以后新增的考试也会显示”“页面只显示你选择分享的内容”等直白表达，减少快照/持续更新歧义。
- 保持正式总分、六科合计、部分科目小计、无记录与未分享等事实边界不变；不改变 Schema 1、Workers KV、Share v2 或 API 契约。
- 新增产品语言计划与回归测试，防止关键内部/AI 风格文案重新进入主页面。

## 0.12.17

- 单科分享改为以“实际记录过该科成绩的考试”为时间轴；没有该科成绩的考试不再作为单科记录、比较对象或趋势节点展示。
- 修正最新一场考试没有该科成绩时，单科分享仍应定位到最近一次实际记录该科成绩的考试，避免出现空白的“当前成绩”。
- 单科轨迹与趋势分析统一复用 `subjectObservationExams`，缺失科目成绩的考试不参与方向、稳定性和变化计算；总成绩与时间轴仍保持完整考试视角。
- 增加“最新考试缺少该科成绩”的回归测试；Schema 1、Workers KV、Share v2 数据结构保持不变。

## 0.12.16

- 新增“多次考试分析语义层”：统一计算当前、上次、首次、最近五次、变化方向、稳定性、数据覆盖度与变化来源，不把一次考试变化误当成长期趋势。
- 单科分析优先选择覆盖足够的同口径指标，避免同一条历史轨迹无提示地混用学校位置、班级排名和分数。
- 私有轨迹首页增加“现在 / 最近几次 / 长期位置 / 变化来源”阅读层；数据不足时明确说明，不制造趋势结论。
- 分享轨迹复用同一分析语义；仅使用分享白名单投影后的数据，未分享的考试范围不会被后台私有数据补回。
- 增加多次考试、单次基线、不可比考试、指标覆盖与变化来源回归测试；Schema 1、Workers KV 与 Share v2 数据结构保持不变。

## 0.12.12

- 收口“成绩事实单一来源”：私有首页的“坐标判断”改用 `record-semantics-v120` 的最近考试、排名状态与比较指标，不再维护第二套排序/指标算法。
- 修正同日考试的动态分析排序：旧考试重新编辑不会改变“最近一次考试”的身份。
- 将坐标判断的 DOM 监听限制在稳定的 `#app` 挂载点，移除对整个页面子树的广泛监听。
- 补充同日排序、班级排名回退与监听范围回归测试；Schema 1、Workers KV、Share v2 数据结构不变。

# Changelog

## 0.12.11

- 修正生产部署契约：Worker `APP_VERSION` 与 `package.json`/lock 的正式版本统一为 0.12.11。
- 本修复只处理发布版本一致性，不改变成绩事实语义和分享逻辑。

## 0.12.10

- 修正公开考试详情与六科概览的白名单布尔方向：只有明确允许的字段才会进入详情内容。
- 修正只分享昵称等最小字段场景下，空 subject projection 不再被误判为已分享。
- 保持“未分享”与“无记录”两种状态严格分离。

## 0.12.9

- 修正公开分享单科字段白名单布尔方向：只有明确允许的成绩/排名字段才显示。
- 修正快照分享中“未分享”字段与正常公开分享字段的区分。

## 0.12.8

- 修正浏览器分享验收 fixture，使其显式携带 share.fields，与真实分享响应的白名单结构一致。
- 保持严格白名单规则：只有字段明确允许时才展示，未授权字段继续显示“未分享”。

## 0.12.7

- 修正分享白名单判定：只有字段明确为 `true` 时才展示，未声明/未授权字段统一按“未分享”处理。
- 修正只分享昵称场景下总体、排名、单科字段的泄露风险与空白状态不一致问题。
- 保持数据 Schema、存储和分享 API 契约不变。

## 0.12.6

- 修正剩余历史版本契约，使测试与实际运行时版本保持同步。
- 本轮不改变成绩事实语义、比较规则或分享行为。

## 0.12.5

- 统一分享页“未分享”和“未记录”的显示契约：白名单未授权字段明确显示未分享，不再混用“待补”。
- 更新浏览器验收契约，验证快照分享在只开放昵称时不会泄露或暗示未分享字段。

## 0.12.4

- 修正 Worker 实际 APP_VERSION 未随版本文件同步的问题，并同步剩余版本契约。
- 本轮不改变成绩事实语义或分享页面行为。

## 0.12.4

- 修正 Worker 实际 `APP_VERSION` 未随版本文件同步的问题，并同步剩余版本契约。
- 本轮不改变成绩事实语义或分享页面行为。

## 0.12.3

- 修正剩余版本契约正则，使 APP_VERSION 检查与当前运行时版本一致。
- 本轮不改变成绩事实语义、比较规则或分享页面行为。

## 0.12.2

- 恢复分享单科页清晰的“某科的历次记录”标题，保持既有用户旅程和浏览器契约。
- 当分享白名单没有任何总体位置字段时，明确显示“位置未分享”，与“有数据但没有记录”区分。
- 本轮仅修正分享展示边界，不改变 Schema 1 或数据存储结构。

## 0.12.1

- 修正 v0.12.0 实施阶段发现的旧考试摘要 createdAt 缺失兼容：加载实际考试实体后再统一稳定排序。
- 补充编辑既有考试时保留原始创建时间的事实测试，以及公开投影中总体/单科排名独立性的测试。
- 继续保持 Schema 1、Workers KV 与 Share v2 契约不变。

## 0.12.0

- 统一编辑页、综合、时间轴、单科与分享页的成绩事实语义；最近一次按考试日期和稳定创建顺序确定，不受旧记录重新编辑影响。
- 校内排名、班级排名、总分和六科成绩作为独立事实保存与读取；只有排名与参考人数同时存在时才计算相对位置。
- 最新考试即使数据未齐也保持为最新，不静默回退到更早的完整考试；无记录的科目在正常阅读页不再显示“—/排名待补”。
- 单科默认采用自动指标：学校相对位置 → 学校排名 → 班级排名 → 分数，并增加最近一次可比科目变化摘要。
- 时间轴与考试详情继续按本场事实展示，不将总体排名与单科排名互相替换。
- 公开分享复用同一事实模型；白名单未分享字段显示“未分享”，已分享但无记录的数据保持留空。
- 修正家庭导航入口写 URL 的连续性，补齐编辑后原 tab/view/subject/metric/exam 上下文保持。
- Schema 1、Workers KV、Share v2 与既有 API 契约保持兼容。

## 0.11.0

- 统一 Share v2 为正式运行时契约，同时兼容既有无 `scope` 的历史分享记录，不需要 KV 迁移。
- 单场分享严格使用用户选中的 `examId`；补充真实 Worker 集成测试覆盖单场、预览、持续轨迹、快照和撤销。
- 单科查看改为“六科概览 → 当前科目焦点”，不再无理由默认语文；私有与公开页面都可通过 URL 恢复当前查看位置。
- 统一轨迹、单科指标、时间轴和主导航的 URL 状态；刷新、前进后退、深链接均保持上下文。
- 编辑考试后返回原来的查看上下文；历史详情比较对象改为该场考试之前最近一场可比考试。
- 变化说明与实际依据一致：用分数时说分数，用学校或班级排名时说排名；回看科目显示实际使用的指标。
- 重新编辑考试时保留 `good / poor / partial / absent` 等原有状态，避免编辑动作隐式改变数据语义。
- 分享字段只接受布尔值，并补充赋分后分数不得超过满分的模型校验。
- Schema 1、既有 KV 结构和生产部署方式保持不变。

## 0.10.0

- 新增独立“坐标判断”分析层：在现有可信成绩、可比考试与相对位置基础上，回答“现在在哪、最近怎么变、变化主要来自哪科、下一次值得继续观察什么”。
- 判断优先学校百分位，其次学校/班级排名，最后才使用分数；缺失、缺考和不可比考试不制造趋势结论。
- 三场以上同口径记录中，重复出现的单科向后会作为“下一次值得继续观察”对象，不生成教学处方或能力判断。
- 私有首页加载分析层，不改变 KV、认证、分享白名单、比较算法、Schema 1 或 API。
- 新增纯函数测试覆盖基线、排名回退、分数回退、类别隔离、缺失数据和特殊考试状态。

## 0.9.4

- 将运行时 CSS 收敛为带版本号的 `/css/app-v094.css` 单一入口，严格保留既有级联顺序，减少首屏资源请求和覆盖不确定性。
- 在 bundle 顶部建立共享设计令牌入口，便于后续逐组件迁移；历史 CSS 源文件继续保留，支持审计和回滚。
- 不改变 KV、认证、分享白名单、比较算法、Schema 或 API；继续保留移动端触控、键盘焦点和显示偏好支持。

## 0.9.3

- 分享页保持克制：移除公开访客看到的“下载 / 分享图片”入口与说明。
- 管理端分享图改为完整分享页长图，包含总成绩、单科成绩和考试时间轴，修复 Windows 下信息过少与文本重叠问题。
- 继续保留一键复制完整地址和打开分享页；不改 KV、认证、白名单或 Schema。

## 0.9.2

- 分享管理页显示完整绝对地址，支持重复复制和直接打开。
- 分享页与管理页支持按白名单生成总成绩、单科或时间轴 PNG；优先系统分享，其次下载，受限浏览器提供 SVG 预览/长按保存。
- 新增登录态分享预览接口，继续复用现有分享白名单与 Schema 1，不返回 secret 原始 token。

## 0.9.1

- 加入 Logo B：成绩记录页、三次考试节点、回看轨迹线与克制的投入提示，统一应用于首页、登录/引导、家庭页和分享页。
- 图形标与“高三坐标”文字分离，不再用“标”字占位；保持低饱和、无游戏化和无数据语义颜色的品牌约束。
- 保持 API、KV、认证、分享字段白名单、比较算法与 Schema（1）不变。

## 0.9.0

- 允许单次记录建立成长轨迹分享，并要求明确确认未来考试会自动加入持续更新链接。
- 分享页稳定呈现总成绩、单科与时间轴；明确快照/持续更新、未分享与待补数据边界。
- 录入支持学生私有回看与下一次小尝试，草稿仅在服务端保存成功后清除。

## 0.8.1

- 外部分享页采用暖纸白、墨线、小面积排线与留白的钢笔手记风格，不影响家庭内页面。
- 总成绩、单科、时间轴在只有一次记录时明确展示“当前基线”，不虚构趋势。
- 修复分享考试详情在窄屏下科目名挤压，以及桌面时间轴箭头换行。
- 新增 Chromium / WebKit 多屏宽、六科导航、时间轴详情、键盘及分享白名单浏览器回归。
- 部署后检查版本、构建 SHA、Schema / KV 契约及首页、分享入口和递归模块资源的逐字节一致性。
- KV、认证、分享字段白名单、比较算法与 Schema / API 保持不变。