# v0.12.28.17 Code Review Security Hardening Plan

Base: 42dc709bd781072c1e5b811ce1c56487416f1434
Branch: security/v01228-postmerge-review
Scope: post-merge code-review remediation; preserve score data Schema 1.

## P0 — Complete one-time recovery credential lifecycle
- Recovery Code reset: consume the Durable Object claim only after the member password/recovery-code/sessionVersion mutation succeeds.
- Recovery Link reset: persist the member mutation and link usedAt/index state, then consume the Durable Object claim.
- Release claims on failed mutations.
- Add regression coverage proving successful reset cannot leave the credential claim reusable.
- Do not weaken the existing Invite/Bootstrap one-time lifecycle.

## P1 — Clarify consistency boundaries
- Document that Workers KV remains eventually consistent and is not a transaction boundary.
- Treat share/exam indexes as rebuildable derived indexes in the follow-up architecture.
- Do not introduce a second storage system in this patch.

## P1 — Remove security-code ambiguity
- Remove dead password-iteration calculation in password-change flow.
- Keep passwordIterations(env) as the single KDF policy source.
- Preserve Cloudflare-compatible PBKDF2 100000 policy for this release.

## P1/P2 — Follow-up architecture (tracked, not mixed into this patch)
- Replace best-effort KV rate counters for security-critical strict quotas with Durable Object or Cloudflare Rate Limiting.
- Add session key-id (kid) rotation support.
- Sunset legacy Secret Share GET-token transport after migration.
- Add structured security events without raw credentials.
- Evaluate mature secret scanning (for example gitleaks) instead of expanding custom regexes.

## Verification gate
1. Source contract tests.
2. Full npm verify.
3. Review exact PR head and all checks.
4. Merge only the exact tested head.
5. Verify main merge SHA.
6. Verify production /api/health appVersion and buildSha when deployment tooling exposes the run.

## Release
- Bump patch revision to v0.12.28.17.
- No score data Schema 1 change.
