import {resolveExamScope,subjectLabel} from "./exam.js";
export const SCORE_SUBJECTS=["chinese","math","english","physics","chemistry","biology"];
export function subjectScore(subject){return subject?.finalScore??subject?.rawScore??null;}
export function examScoreSummary(exam){if(exam?.scoreSummary?.kind)return exam.scoreSummary;const scope=resolveExamScope(exam),expectedSubjectKeys=scope.subjects;const values=expectedSubjectKeys.map((key)=>subjectScore(exam?.subjects?.[key])).filter((value)=>value!=null);const recordedSubjects=values.length;const subtotal=recordedSubjects?Math.round(values.reduce((sum,value)=>sum+Number(value),0)*10)/10:null;const official=exam?.overall?.officialScore??exam?.officialScore??null;if(exam?.attendance==="absent"||exam?.status==="absent")return{kind:"absent",value:null,subtotal,recordedSubjects,expectedSubjects:expectedSubjectKeys.length,expectedSubjectKeys,complete:false,label:"本场缺考"};if(official!=null)return{kind:"official",value:official,subtotal,recordedSubjects,expectedSubjects:expectedSubjectKeys.length,expectedSubjectKeys,complete:recordedSubjects===expectedSubjectKeys.length,label:"总分"};if(recordedSubjects===expectedSubjectKeys.length){const label=expectedSubjectKeys.length===6?"六科合计":expectedSubjectKeys.length===1?subjectLabel(expectedSubjectKeys[0]):"本次科目合计";return{kind:"calculated_complete",value:subtotal,subtotal,recordedSubjects,expectedSubjects:expectedSubjectKeys.length,expectedSubjectKeys,complete:true,label};}if(recordedSubjects){const label=expectedSubjectKeys.length===6?recordedSubjects+"/6 科小计":"已录 "+recordedSubjects+"/"+expectedSubjectKeys.length+" 科";return{kind:"calculated_partial",value:null,subtotal,recordedSubjects,expectedSubjects:expectedSubjectKeys.length,expectedSubjectKeys,complete:false,label};}return{kind:"missing",value:null,subtotal:null,recordedSubjects:0,expectedSubjects:expectedSubjectKeys.length,expectedSubjectKeys,complete:false,label:expectedSubjectKeys.length===6?"总分待补":"成绩待补"};}
export function scoreConsistency(exam){const stored=exam?.overall?.scoreConsistency;if(stored?.status)return stored;const summary=examScoreSummary(exam),official=exam?.overall?.officialScore??exam?.officialScore??null,complete=summary.expectedSubjects>0&&summary.recordedSubjects===summary.expectedSubjects;if(official==null||summary.subtotal==null||!complete)return{status:"not_checked",officialScore:official,calculatedScore:summary.subtotal,delta:null};const delta=Math.round((summary.subtotal-official)*10)/10;return{status:Math.abs(delta)<0.05?"match":"mismatch",officialScore:official,calculatedScore:summary.subtotal,delta};}
